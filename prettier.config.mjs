/** @type {PrettierConfig} */
export default {
  arrowParens: 'avoid',
  printWidth: 100,
  singleQuote: true,
  trailingComma: 'es5',
};

/**
 * @typedef {import('prettier').Config} PrettierConfig
 */

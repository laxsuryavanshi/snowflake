{
  "name": "@turtleby/idms-api",
  "private": true,
  "packageManager": "yarn@4.9.1",
  "scripts": {
    "build": "run mvn:cli clean package",
    "clean": "run mvn:cli clean",
    "package": "run mvn:cli package"
  }
}
